# PhotoStudio

## Dockerファイルの起動
`　docker-compose up -d　`でDockerファイルを起動

## データベースの作成
`http://localhost:8888/`にアクセス

```
ユーザ名: root
パスワード： secret
```
でログイン  

画面下部からコンソールを開き
`photostudio.sql`ファイルのコマンドを入力

## 