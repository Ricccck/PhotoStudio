<!DOCTYPE html>

<html lang="ja">
	<head>
		<meta charset="utf-8"/>
		<meta
		name="viewport" content="width=device-width, initial-scale=1">

		{# Bootstrap #}
		<link
		href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-9ndCyUaIbzAi2FUVXJi0CjmCapSmO7SnpJef0486qhLnuZ2cdeRhO02iuK6FUUVM" crossorigin="anonymous">

		{# favicon #}
		<link
		rel="icon" href="/public/favicon-black.ico"/>

		{# javascript #}
		<script src="js/changeFavicon.js"></script>
		<script src="https://yubinbango.github.io/yubinbango/yubinbango.js" charset="UTF-8"></script>

		{# css #}
		<link href="css/common.css" rel="stylesheet" type="text/css"/>
		<link
		href="css/download.css" rel="stylesheet" type="text/css"/>

		{# title #}
		<title>PhotoStudio -ダウンロード-</title>
	</head>

	<body>
		{% include 'common/header.html.twig' %}

		<div class="contents justify-content-start">
			<h1 class="mt-3 mb-3 ">
				購入商品ダウンロード
			</h1>

			<div id="downloadAccordion" class="accordion">
				{% for item in crtArr %}
					<div class="accordion-item">
						<h2 class="accordion-header">
							<button class="accordion-button" type="button" data-bs-toggle="collapse" data-bs-target="#{{item.crt_id}}" aria-expanded="true" aria-controls="{{item.crt_id}}">
								<div class="d-flex justify-content-between w-100">
									<span>{{item.photo_title}}</span>
									<img src="public/upload/{{item.photo_url}}" alt="{{item.photo_title}}" class="img-fluid me-4"/></div>
							</button>
						</h2>

						<div id="{{item.crt_id}}" class="accordion-collapse collapse" data-bs-parent="#downloadAccordion">
							<div class="accordion-body">
								<img src="public/upload/{{item.photo_url}}" alt="{{item.photo_title}}" class="img-fluid"/>

								<div class="mb-2">
									<label>カテゴリー</label>
									<h5>{{item.category}}</h5>
								</div>

								<div class="mb-2">
									<label>タグ</label>
									<div>
										{% for tag in item.tags %}
											<span class="badge text-bg-white text-dark border border-dark">{{tag}}</span>
										{% endfor %}
									</div>
								</div>

								<div class="mb-2">
									<label>価格</label>
									<h5>
										¥<span id="price">{{item.price}}</span>-
									</h5>
								</div>

								<div class="mb-2">
									<label>投稿日時</label>
									<h6>{{item.upload_at}}</h6>
								</div>

								<div class="mb-2">
									<label>購入日時</label>
									<h6>{{item.purchased_at}}</h6>
								</div>

								<a class="btn btn-primary w-100 flex-grow-1" href="download.php?photo_id={{item.photo_id}}">
									ダウンロードする
								</a>
							</div>
						</div>
					</div>
				</li>
			{% endfor %}
		</div>
	</body>
</html></div>{# Bootstrap #}<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js" integrity="sha384-geWF76RCwLtnZ8qwWowPQNguL3RmwHVBC9FhGdlKrxdiJJigb/j/68SIy3Te4Bkz" crossorigin="anonymous"></script></body></html><h2 class="accordion-header"><button class="accordion-button" type="button" data-bs-toggle="collapse" data-bs-target="#id" aria-expanded="true" aria-controls="id"><div class="d-flex justify-content-between w-100">
<span>タイトル</span>
<img src="/path" class="img-fluid me-4"/></div></button></h2>
