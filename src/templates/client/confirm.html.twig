<!DOCTYPE html>

<html lang="ja">
	<head>
		<meta charset="utf-8"/>
		<meta
		name="viewport" content="width=device-width, initial-scale=1">

		{# Bootstrap #}
		<link
		href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-9ndCyUaIbzAi2FUVXJi0CjmCapSmO7SnpJef0486qhLnuZ2cdeRhO02iuK6FUUVM" crossorigin="anonymous">

		{# favicon #}
		<link
		rel="icon" href="/public/favicon-black.ico"/>

		{# javascript #}
		<script src="js/changeFavicon.js"></script>
		<script src="https://yubinbango.github.io/yubinbango/yubinbango.js" charset="UTF-8"></script>

		{# css #}
		<link href="css/common.css" rel="stylesheet" type="text/css"/>
		<link
		href="css/confirm.css" rel="stylesheet" type="text/css"/>

		{# title #}
		<title>PhotoStudio -登録情報確認-</title>
	</head>

	<body>
		{% include 'common/sub_header.html.twig' %}


		<div class="contents">
			<div class="card">
				<div class="card-header text-center">
					<h1>登録情報</h1>
				</div>
				<div class="mt-3 card-body">
					<form method="post" action="cliConfirm.php">
						{% for key, value in dataArr %}
							<input type="hidden" name="{{key}}" value="{{value}}">
						{% endfor %}

						<div class="mb-3">
							<label for="username">
								ユーザーネーム
							</label>
							<h4 id="username">
								{{dataArr.username}}
							</h4>
						</div>

						<div class="mb-3">
							<label for="name">
								氏名
							</label>
							<div id="name" class="d-flex p-2">
								<label for="last_name">
									（姓）
								</label>
								<h4 id="last_name">
									{{dataArr.last_name}}
								</h4>
								<label for="first_name">
									（名）
								</label>
								<h4 id="first_name">
									{{dataArr.first_name}}
								</h4>
							</div>
						</div>

						<div class="mb-3">
							<label for="name_kana">
								氏名（かな）
							</label>
							<div id="name_kana" class="d-flex p-2">
								<label for="last_name_kana">
									（姓）
								</label>
								<h4 id="last_name_kana">
									{{dataArr.last_name_kana}}
								</h4>
								<label for="first_name_kana">
									（名）
								</label>
								<h4 id="first_name_kana">
									{{dataArr.first_name_kana}}
								</h4>
							</div>
						</div>

						<div class="mb-3">
							<label for="company_name">
								会社名
							</label>
							<h4 id="company_name">
								{{dataArr.company_name}}
							</h4>
						</div>

						<div class="mb-3">
							<label for="email">
								メールアドレス
							</label>
							<h4 id="email">
								{{dataArr.email}}
							</h4>
						</div>

						<div class="mb-3">
							<label for="phone_number">
								電話番号
							</label>
							<h4 id="phone_number">
								{{dataArr.tel1}}-{{dataArr.tel2}}-{{dataArr.tel3}}
							</h4>
						</div>

						<div class="mb-3">
							<label for="sex">
								性別
							</label>
							<h4 id="sex">{{sexArr[dataArr.sex]}}</h4>
						</div>

						<div class="mb-3">
							<label for="zip">
								郵便番号
							</label>
							<h4 id="zip">{{dataArr.zip}}</h4>
						</div>

						<div class="mb-3">
							<label for="address">
								住所
							</label>
							<h4 id="address">
								{{dataArr.pref}}{{dataArr.city}}{{dataArr.town}}
							</h4>
						</div>

						<div class="mb-3">
							<label for="website">
								ホームページ
							</label>
							<h4 id="website">{{dataArr.website}}</h4>
						</div>

						<div class="mb-3">
							<label for="password">
								パスワード
							</label>
							<h4 id="password">{{dataArr.password}}</h4>
						</div>

						<div class="mb-3">
							<p class="red">ユーザーネーム・メールアドレスは一度登録すると変更できません</p>
						</div>

						<div class="d-flex gap-2 mt-4 mb-4">
							<button type="submit" name="complete" class="btn btn-primary btn-lg w-100 flex-grow-1">登録完了</button>
							<button type="submit" name="back" class="btn btn-secondary btn-lg w-100 flex-grow-1">戻る</button>
						</div>
					</form>
				</div>
			</div>
		</div>


		{# Bootstrap #}
		<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js" integrity="sha384-geWF76RCwLtnZ8qwWowPQNguL3RmwHVBC9FhGdlKrxdiJJigb/j/68SIy3Te4Bkz" crossorigin="anonymous"></script>
	</body>
</html>
