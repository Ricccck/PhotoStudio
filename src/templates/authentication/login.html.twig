<!DOCTYPE html>

<html lang="ja">
	<head>
		<meta charset="utf-8"/>
		<meta
		name="viewport" content="width=device-width, initial-scale=1">

		{# Bootstrap #}
		<link
		href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-9ndCyUaIbzAi2FUVXJi0CjmCapSmO7SnpJef0486qhLnuZ2cdeRhO02iuK6FUUVM" crossorigin="anonymous">

		{# favicon #}
		<link
		rel="icon" href="/public/favicon-black.ico"/>

		{# javascript #}
		<script src="js/changeFavicon.js"></script>

		{# css #}
		<link href="css/common.css" rel="stylesheet" type="text/css"/>
		<link
		href="css/login.css" rel="stylesheet" type="text/css"/>

		{# title #}
		<title>PhotoStudio -ログイン-</title>
	</head>

	<body>
		{% include 'common/sub_header.html.twig' %}


		<div class="contents">
			<div class="card">
				<div class="card-header text-center">
					<h1>ログイン</h1>
				</div>
				<div class="card-body">
					<form method="post" action="login.php">
						<div class="mb-3">
							<label for="email" class="form-label">
								メールアドレス<span class="red">*</span>
							</label>
							<input type="email" id="email" name="email" class="form-control" size="30" placeholder="もしくはユーザーIDを入力"/>
							{% if errArr.email | length > 0 %}
								<div class="red">{{errArr.email}}</div>
							{% endif %}
							{% if errArr.username | length > 0 %}
								<div class="red">{{errArr.username}}</div>
							{% endif %}
						</div>
						<div class="mb-3">
							<label for="password" class="form-label">
								パスワード<span class="red">*</span>
							</label>
							<input type="password" id="password" name="password" class="form-control" size="30"/>
							{% if errArr.password | length > 0 %}
								<div class="red">{{errArr.password}}</div>
							{% endif %}
						</div>
						<div class="mb-3">
							<div class="form-check form-check-inline">
								<input type="radio" id="customer" class="form-check-input" name="user" value="customer"/>
								<label for="customer" class="form-check-label">購入者</label>
							</div>
							<div class="form-check form-check-inline">
								<input type="radio" id="customer" class="form-check-input" name="user" value="client"/>
								<label for="client" class="form-check-label">販売者</label>
							</div>
							{% if errArr.user | length > 0 %}
								<div class="red">{{errArr.user}}</div>
							{% endif %}
						</div>
						<div class="d-grid">
							<input type="submit" name="login" class="btn btn-primary btn-lg" value="ログイン" size="30"/>
							{% if errArr.login | length > 0 %}
								<div class="red">{{errArr.login}}</div>
							{% endif %}
						</div>
					</form>
				</div>
			</div>
		</div>
		{# Bootstrap #}

		<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js" integrity="sha384-geWF76RCwLtnZ8qwWowPQNguL3RmwHVBC9FhGdlKrxdiJJigb/j/68SIy3Te4Bkz" crossorigin="anonymous"></script>
	</body>
</html>
