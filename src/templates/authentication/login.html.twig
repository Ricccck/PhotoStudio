<!DOCTYPE html>
<html>
	<head>
		<meta charset="utf-8"/>
		<meta name="viewport" content="width=device-width, initial-scale=1">

		{# Bootstrap #}
		<link
		href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-9ndCyUaIbzAi2FUVXJi0CjmCapSmO7SnpJef0486qhLnuZ2cdeRhO02iuK6FUUVM" crossorigin="anonymous">

		{# favicon #}
		<link
		rel="icon" href="/public/favicon-black.ico"/>

		{# javascript #}
		<script src="js/changeFavicon.js"></script>

		{# css #}
		<link href="css/common.css" rel="stylesheet" type="text/css"/>
		<link
		href="css/login.css" rel="stylesheet" type="text/css"/>

		{# title #}
		<title>PhotoStudio -ログイン-</title>
	</head>

	<body>
		{% include 'common/sub_header.html.twig' %}


		<div class="login-body contents">
			<div class="card">
				<h1 id="title">ログイン</h1>
				<div id="login-form">
					<form method="post" action="login.php">
						<div id="mail-input">
							<div>メールアドレス<span class="red">*</span>
							</div>
							<div>
								<input class="input" type="text" name="email" value="" size="30">
								{% if errArr.email | length > 0 %}
									<div class="red">{{errArr.email}}</div>
								{% endif %}
							</div>
						</div>
						<div id="pass-form">
							<div id="pass-titile">パスワード<span class="red">*</span>
							</div>
							<div id="pass-input">
								<input class="input" type="password" name="password" value="" size="30">
								{% if errArr.password | length > 0 %}
									<div class="red">{{errArr.password}}</div>
								{% endif %}
							</div>
						</div>
						<div id="which-user">
							<input type="checkbox" id="check-customer" name="customer"/>
							<label for="customer">購入者</label>
							<input type="checkbox" id="check-client" name="client"/>
							<label for="client">販売者</label>
							{% if errArr.check | length > 0 %}
								<div class="red">{{errArr.check}}</div>
							{% endif %}
						</div>
						<div id="submit-btn">
							<input type="submit" name="login" value="ログイン">
							{% if errArr.login | length > 0 %}
								<div class="red">{{errArr.login}}</div>
							{% endif %}
						</div>
					</form>
				</div>
			</div>
		</div>

		{# Bootstrap #}
		<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js" integrity="sha384-geWF76RCwLtnZ8qwWowPQNguL3RmwHVBC9FhGdlKrxdiJJigb/j/68SIy3Te4Bkz" crossorigin="anonymous"></script>
	</body>
</html>
